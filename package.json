{
  "name": "@statenlogic/for-of-walk",
  "version": "1.0.0-beta.1",
  "description": "A file walker that uses asynchronous iterators. It recursively explores files and directories in a file system and returns them as an iterable sequence. Useful when you want to list the files in a directory and its subdirectories.",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/**/*.test.js"
  },
  "author": "Alexander Curtis",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/alexandercurtis/for-of-walk"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.0.0"
  }
}
